function rectangleAreaCalculation(width, height) {
    let area = width * height;
    return area;
}

console.log(rectangleAreaCalculation(7, 5));

const rectAreaCalc = function (width, height) {
    let rectArea = width * height;
    return rectArea;
};

console.log(rectAreaCalc(80, 10));

let calcAreaRect = (width, height) => width * height;

console.log(calcAreaRect(1000, 50));
